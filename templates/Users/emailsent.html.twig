{% extends 'base2.html.twig' %}

{% block content %}

    <div class="confirmation-message">
        <div class="card">
            <div class="card-header">
                <div class="card-body">
                    <h2>An email has been sent to <span>{{ email }}</span> containing a link to reset your password.</h2>
                </div>
            </div>
        </div>
    </div>

{% endblock %}